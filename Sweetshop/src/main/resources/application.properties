spring.application.name=Sweetshop

spring.datasource.url=${DB_URL:jdbc:mysql://127.0.0.1:3306/sweetshop?useSSL=false&serverTimezone=UTC}
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:shreya123}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

server.port=${SERVER_PORT:8080}

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true

# JWT Configuration
jwt.secret=${JWT_SECRET:SweetShopSecretKeySweetShopSecretKey12345}
jwt.expiration=${JWT_EXPIRATION:86400000}
